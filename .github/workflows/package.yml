name: Package
on:
  push:
    branches: [ master ]
jobs:
  test:
    runs-on: macos-latest
    steps:
    - name: checkout
      uses: actions/checkout@v2
    - name: secrets
      id: secrets
      env:
        github: ${{secrets.GH_AUTH}}
        infura: ${{secrets.INFURA_TOKEN}}
      run: echo $infura
